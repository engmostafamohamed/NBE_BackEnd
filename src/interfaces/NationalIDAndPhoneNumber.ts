export interface INationalIDAndPhoneNumber {

    nationalID: number;
    phoneNumber:string
    // role: string;
    // verified: boolean;
    // status: string;
    // // role: "user" | "admin" | "customer" | "merchant" | "branch_manager";
    createdAt?: Date;
    updatedAt?: Date;
}

